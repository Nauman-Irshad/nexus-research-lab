# Optional: JSON form-handling endpoint for the contact / join forms.
# Without this, the form opens a pre-filled mailto: draft instead.
NEXT_PUBLIC_CONTACT_ENDPOINT=

# Optional override for the visual QA screenshot script.
# CHROME_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe
# BASE_URL=http://localhost:3000
